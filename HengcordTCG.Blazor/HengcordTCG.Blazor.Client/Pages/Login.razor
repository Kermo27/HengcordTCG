@page "/login"
@using System
@inject AuthService AuthService
@inject NavigationManager Navigation
@inject IJSRuntime JS

<PageTitle>Login - HengcordTCG</PageTitle>

<div class="min-h-[calc(100vh-4rem)] flex items-center justify-center p-4">
    @if (IsProcessing)
    {
        <div class="text-center">
            <i class="fa fa-spinner fa-spin text-4xl text-amber-400 mb-4"></i>
            <p class="text-slate-400">Logging you in...</p>
        </div>
    }
    else
    {
        <div class="w-full max-w-md bg-slate-900/50 border border-slate-800 rounded-xl p-8 text-center">
            <div class="mx-auto w-20 h-20 rounded-full bg-gradient-to-br from-indigo-500 to-purple-500 flex items-center justify-center mb-6">
                <i class="fa fa-sparkles text-white text-3xl"></i>
            </div>
            
            <h1 class="text-2xl font-bold text-white mb-2">Welcome to HengcordTCG</h1>
            <p class="text-slate-400 mb-6">Sign in to collect, trade, and battle!</p>
            
            <button @onclick="HandleLogin" class="w-full bg-gradient-to-r from-indigo-500 to-purple-500 hover:from-indigo-600 hover:to-purple-600 text-white font-medium py-3 rounded-lg">
                <i class="fa fa-discord mr-2"></i> Login with Discord
            </button>
        </div>
    }
</div>

@code {
    private bool IsProcessing = true;

    protected override async Task OnInitializedAsync()
    {
        var uri = Navigation.ToAbsoluteUri(Navigation.Uri);
        var query = uri.Query;
        
        if (!string.IsNullOrEmpty(query) && query.Contains("token="))
        {
            var tokenStart = query.IndexOf("token=") + 6;
            var token = query.Substring(tokenStart).Split('&')[0];
            token = Uri.UnescapeDataString(token);

            if (!string.IsNullOrEmpty(token))
            {
                await AuthService.SetTokenAsync(token);
                var user = await AuthService.GetCurrentUserAsync();
                if (user != null)
                {
                    Navigation.NavigateTo("/");
                    return;
                }
            }
        }

        var jwtToken = AuthService.GetToken();
        if (!string.IsNullOrEmpty(jwtToken))
        {
            var user = await AuthService.GetCurrentUserAsync();
            if (user != null)
            {
                Navigation.NavigateTo("/");
                return;
            }
        }

        IsProcessing = false;
    }

    private void HandleLogin()
    {
        var loginUrl = AuthService.GetDiscordLoginUrl();
        Navigation.NavigateTo(loginUrl, forceLoad: true);
    }
}
